# 📋 AI Timeline 事件卡片产品规格文档

**版本**: 1.0  
**更新日期**: 2026-04-20  
**文档类型**: 产品需求规格说明书 (PRD)

---

## 一、卡片定位与目标

### 1.1 产品定位

**事件聚合卡片 / 情报事件卡片 / 时间轴事件节点卡片**

本卡片承载的不是单篇文章，而是一个已经经过初步聚合、筛选、排序后的**"事件"**。

### 1.2 核心职责

每张卡片同时表达 5 件事：

1. ✅ **这是什么事件** - 标题区
2. ✅ **它来自哪里** - 来源信息区
3. ✅ **它大概讲了什么** - 摘要区
4. ✅ **它被多少来源共同印证** - 聚合提示区
5. ✅ **为什么系统认为它值得关注** - 推荐理由区

### 1.3 设计原则

- **非传统新闻卡片** - 不是"封面图 + 标题 + 摘要"结构
- **偏文本密集型** - 信息密度高
- **分析型导向** - 强调系统分析与判断
- **情报型结构** - 专业、克制、连续追踪

---

## 二、页面布局架构

### 2.1 三段式结构

```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏                                             │
│  [Logo] 搜索框 [状态]                                   │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  左侧边栏  │  主内容区                                  │
│  窄栏导航  │  ┌──────────────────────────────┐          │
│  情报分类  │  │ 时间轴 (骨架)               │          │
│  信号标签  │  │ |                           │          │
│  数据来源  │  │ 22:45 ●─────[事件卡片]      │          │
│  统计信息  │  │ |                           │          │
│            │  │ 22:32 ●─────[事件卡片]      │          │
│            │  │ |                           │          │
│            │  │ 21:03 ●─────[事件卡片]      │          │
│            │  └──────────────────────────────┘          │
└────────────┴────────────────────────────────────────────┘
```

### 2.2 卡片与时间轴关系

| 元素 | 位置 | 作用 |
|------|------|------|
| 大号时间 | 左侧 | 22:45 / 22:32 / 21:03 |
| 时间轴节点 | 中间 | 发光圆点 |
| 事件卡片 | 右侧 | 挂接在时间轴上 |

**关键设计**：
- 时间轴是骨架
- 卡片是时间节点对应的主体内容
- 用户阅读顺序：先看时间 → 再看事件卡片

---

## 三、卡片视觉规范

### 3.1 外观样式

#### 轮廓
- ✅ **大圆角矩形** - `border-radius: 16px`
- ✅ **深色底** - `background: rgba(19, 27, 51, 0.7)`
- ✅ **轻描边** - `border: 1px solid rgba(255, 255, 255, 0.08)`
- ✅ **极弱阴影** - `box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4)`
- ✅ **贴合背景但略微浮起**

#### 视觉感受
> "一块悬浮在暗色情报面板上的半透明信息板"

**关键词**：
- 深色玻璃卡
- 低对比边框
- 轻悬浮
- 非强分割
- 高级克制

### 3.2 色彩系统

#### 卡片底色
- **不是纯黑，也不是纯蓝**
- **深蓝黑** - `#131b33`
- **带冷色偏移**
- **比页面主背景略亮**

**好处**：
- 卡片能从背景中分离出来
- 但不会显得突兀
- 整体保持统一的夜间科技感

#### 边框
- **非常轻**
- 细线：`1px`
- 透明度低：`rgba(255, 255, 255, 0.08)`
- 偏青蓝/灰蓝：`border-color: var(--border-light)`

**作用**：
- 勾出卡片轮廓
- 增加精致感
- 配合深色背景避免糊成一片

#### 圆角
- **比较大** - `16px`
- 明显现代化风格
- 不尖锐
- 不传统方卡
- 更柔和、更高级

在高信息密度下不会太"硬"。

---

## 四、卡片内部结构（6 层信息架构）

### 4.1 第一层：顶部来源信息区

**位置**：卡片最上方  
**作用**：交代"这条事件主参考源来自哪里"

#### 内容组成
```tsx
{platform_name} · 查看原文 · 排名 #{rank}
```

**示例**：
- "知乎 · 查看原文 · 排名 #1"
- "Hacker News · 查看原文 · 排名 #3"

#### 视觉特征
- 字号：`text-xs` (12px)
- 颜色：`text-muted` (#6b7280)
- 来源名：`text-text-secondary` (稍强调)
- 放在最顶部
- 明显弱于标题

#### 设计目的
这是"出处说明"，不是"阅读重点"。

告诉用户：
- 这条事件主信号是从哪来的
- 当前主展示文本基于哪个来源聚合出来

---

### 4.2 第二层：主标题区

**位置**：来源行下方  
**作用**：一句话表达"发生了什么"

#### 示例文案
- "Claude Opus 4.7 正式发布"
- "Qwen3.6-35B-A3B：具有能动编码能力的模型，现已向所有人开放"

#### 视觉特征
- 字号：`text-xl` (20px)
- 字重：`font-bold`
- 对比：最强（text-text-primary）
- 行控制：通常单行或双行
- **是卡片内第一视觉焦点**

#### 内容特点
- 不是媒体文章标题的夸张写法
- 偏向：**事件标题 / 情报标题 / 归纳标题**
- 经过系统整理后的结果
- 不是照搬原始文章标题

**这非常适合情报产品定位**。

---

### 4.3 第三层：摘要区

**位置**：主标题下方  
**作用**：快速理解事件概况

#### 内容特点
通常包括：
- ✅ 版本变化
- ✅ 性能变化
- ✅ 影响范围
- ✅ 产品特征
- ✅ 关键数字
- ✅ 额外背景

**示例**：
> "性能提升 30%，支持 128K 上下文窗口，新增代码执行能力，定价保持不变..."

#### 视觉特征
- 字号：`text-sm` (14px)
- 颜色：`text-text-secondary` (#b4bcc4)
- 行高：`leading-relaxed` (较大)
- 长度：2~4 行

#### 设计定位
> 系统整理出的"事件摘要"

而不是文章正文，也不是纯摘录。

---

### 4.4 第四层：标签区

**位置**：摘要下方  
**作用**：识别事件领域、主体和类型

#### 标签分类

**第一层：主题标签**
- Agent
- 编码
- 推理
- 开源

**第二层：品牌/主体标签**
- Anthropic
- OpenAI
- Google
- 阿里云

**第三层：事件类型标签**
- 模型发布
- 论文
- 产品更新
- 融资
- 政策

#### 视觉样式
```css
.tag-pill {
  background: rgba(19, 27, 51, 0.8);
  border: 1px solid rgba(255, 255, 255, 0.08);
  color: #b4bcc4;
  border-radius: 9999px;
  padding: 4px 10px;
  font-size: 12px;
}
```

**特点**：
- 小胶囊
- 深色底
- 细边框
- 小字号
- 不抢眼

#### 设计原则
标签不是做筛选主入口，而是做**快速识别辅助**。

帮助用户秒懂这个事件属于什么领域，但不应该比标题更重要。

---

### 4.5 第五层：聚合提示区 ⭐

**位置**：标签区下方  
**文案**：
> "另有 N 个来源也报道了此事件"

#### 核心价值

**这是最关键的区块，体现产品的"事件聚合"定位**。

**它表达的是**：
- 当前卡片并不是一篇单独文章
- 而是一个**事件簇**
- 系统已经识别到有多个来源共同指向它

#### 把产品从"资讯列表"升级成"事件聚合系统"

#### 视觉特征
```css
.source-aggregate-bar {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 8px;
  padding: 10px 14px;
  font-size: 13px;
  color: #b4bcc4;
}
```

- 独立一行
- 字号小于标题大于来源
- 轻微底色或边框
- 位置在卡片中下部
- 不高调，但很关键

#### 可交互扩展
**点击展开** → 查看：
- 哪些来源跟进了
- 相似标题有哪些
- 哪个来源最早发
- 各来源的说法差异

---

### 4.6 第六层：底部推荐条（系统判断）

**位置**：卡片最底部  
**样式**：横向整条铺开的绿色信息条

#### 文案示例
> **推荐理由**：开发者可将最难的编码任务完全托管...

#### 核心作用
表达的是：

**系统为什么把这条事件放到你面前**

不是只告诉你"这件事发生了"，而是告诉你：
- ✅ 为什么重要
- ✅ 为什么值得看
- ✅ 对用户可能意味着什么

#### 本质：机器解释层

相当于给事件卡再加一层**"策展理由"**。

#### 设计意义

| 没有此条 | 有此条 |
|---------|-------|
| 页面只是"聚合器" | 页面变成"聚合 + 筛选 + 解释" |
| 被动展示信息 | 主动提供判断 |
| 用户自己筛选 | 系统辅助决策 |

**这对个人雷达产品非常重要**。

#### 视觉样式
```css
.recommendation-bar {
  background: linear-gradient(
    90deg,
    rgba(0, 255, 136, 0.08) 0%,
    rgba(0, 255, 136, 0.05) 100%
  );
  border-left: 3px solid #00ff88;
  border-radius: 8px;
  padding: 12px 16px;
  box-shadow: inset 0 0 24px rgba(0, 255, 136, 0.2);
}
```

**特点**：
- 横向全宽
- 绿色/青绿色半透明底
- 有轻微渐变
- 与主卡片正文形成色彩分层
- 很像系统生成的洞察结论

---

### 4.7 第七层：右上角操作区

**组成**：
1. **事件信号分** (Radar Score)
2. **收藏/星标按钮**

#### 事件信号分

**定义**：
- 不是"热度"这种泛概念
- 而是 **事件信号分 / Radar Score / Event Score**

**综合因素**：
- 来源数量
- 来源权重
- 热度增长
- 官方源占比
- 近时段讨论增量

#### 视觉样式
```css
.heat-badge {
  width: 48px;
  height: 48px;
  border-radius: 12px;
  background: linear-gradient(135deg, rgba(0, 212, 255, 0.15), rgba(0, 168, 204, 0.1));
  border: 1px solid rgba(0, 212, 255, 0.3);
  color: #00d4ff;
  box-shadow: 0 4px 16px rgba(0, 212, 255, 0.3);
}

.heat-badge.high {
  background: linear-gradient(135deg, rgba(0, 255, 136, 0.15), rgba(0, 200, 100, 0.1));
  border-color: rgba(0, 255, 136, 0.4);
  color: #00ff88;
  box-shadow: 0 4px 16px rgba(0, 255, 136, 0.3);
}
```

---

## 五、卡片层级顺序（浏览路径）

用户的自然阅读顺序：

```
1. 看时间 (22:45)         ← 左侧大号时间
         ↓
2. 看标题 (发生了什么)     ← 卡片主标题
         ↓
3. 看摘要 (大概内容)       ← AI 摘要
         ↓
4. 看标签 (事件分类)       ← 标签胶囊
         ↓
5. 看聚合提示 (多少来源)   ← "另有 N 个来源"
         ↓
6. 看推荐理由 (为什么重要) ← 绿色推荐条
         ↓
7. 看分数/操作 (权重判断)  ← 信号分 + 收藏
```

**这是一个非常顺的浏览链路**。

---

## 六、文案风格指南

### 6.1 整体风格

不是媒体编辑腔，也不是社交口语，而是：

- ✅ 冷静
- ✅ 归纳式
- ✅ 克制
- ✅ 信息密度高
- ✅ 少修辞
- ✅ 重事实

### 6.2 各区域文案

| 区域 | 风格 | 示例 |
|------|------|------|
| 标题 | 事件归纳标题 | "Claude Opus 4.7 正式发布" |
| 摘要 | 系统总结 | "性能提升 30%，支持 128K 上下文..." |
| 标签 | 语义分类标签 | Agent / 模型发布 / 编码 |
| 聚合提示 | 系统提示语气 | "另有 10 个来源也报道了此事件" |
| 推荐理由 | 价值解释 | "开发者可将最难编码任务完全托管..." |

---

## 七、交互设计

### 7.1 聚合提示展开

**状态**：
- 默认：折叠状态
- 点击：展开查看详情

**展开后内容**（预留）：
```tsx
{isExpanded && (
  <div className="mt-3 pt-3 border-t">
    {/* 来源列表 */}
    <ul>
      {sources.map(source => (
        <li>{source.name} - {source.time}</li>
      ))}
    </ul>
    
    {/* 相似标题对比 */}
    <div>其他报道角度...</div>
    
    {/* 时间分布 */}
    <div>最早发布时间：...</div>
  </div>
)}
```

### 7.2 收藏操作

- 点击星标：切换收藏状态
- 视觉反馈：图标填充 +霓虹发光
- 数据同步：更新数据库

### 7.3 外部链接

- 来源行"查看原文"：新窗口打开
- 保持卡片在当前页面
- 不影响浏览连续性

---

## 八、信息架构总结

### 8.1 标准版卡片

```
1. 来源行 (来源/排名)
2. 标题 (事件名称)
3. 摘要 (系统总结)
4. 标签 (分类识别)
5. 聚合提示 (事件簇说明)
6. 推荐理由 (系统判断)
7. 评分/操作 (信号分 + 收藏)
```

### 8.2 更强情报版（未来扩展）

```
1. 来源行
2. 事件标题
3. 事件摘要
4. 标签
5. 来源分布说明
6. 相似信号数
7. 推荐理由
8. Radar Score
9. 展开查看详情
```

---

## 九、核心设计特征（5 条）

### 9.1 它是事件卡，不是文章卡 ⭐

**根本区别**。

| 事件卡 | 文章卡 |
|-------|-------|
| 对应一个事件簇 | 对应单篇文章 |
| 多来源聚合 | 单一来源 |
| 系统归纳标题 | 原始标题 |
| 推荐理由 | 无 |

### 9.2 它依附时间轴存在

不是独立 feed，而是挂在时间轴右侧的事件节点。

### 9.3 它中部强调"聚合提示"

体现系统已经做了事件归并。

### 9.4 它底部强调"推荐理由"

体现系统有筛选判断能力。

### 9.5 它整体是深色玻璃科技感

适合高密度文本与连续浏览。

---

## 十、技术实现

### 10.1 组件结构

```tsx
<NewsCard>
  {/* 1. 来源信息区 */}
  <div className="text-xs text-text-muted">来源 · 排名</div>
  
  {/* 2. 主标题区 */}
  <h2 className="text-xl font-bold">{title}</h2>
  
  {/* 3. 摘要区 */}
  <p className="text-text-secondary">{reason}</p>
  
  {/* 4. 标签区 */}
  <div className="flex flex-wrap gap-2">{tags}</div>
  
  {/* 5. 聚合提示区 */}
  {related_count > 0 && (
    <div className="source-aggregate-bar">
      另有 {related_count} 个来源
    </div>
  )}
  
  {/* 6. 推荐条 */}
  {heat_score >= 75 && (
    <div className="recommendation-bar">
      推荐理由...
    </div>
  )}
  
  {/* 7. 操作区 */}
  <div className="absolute top-4 right-4">
    <div className="heat-badge">{score}</div>
    <button>收藏</button>
  </div>
</article>
```

### 10.2 CSS 变量

```css
:root {
  --bg-primary: #0a0e1a;
  --bg-card: #131b33;
  --accent-primary: #00d4ff;
  --accent-green: #00ff88;
  --text-primary: #ffffff;
  --text-secondary: #b4bcc4;
  --text-muted: #6b7280;
  --border-light: rgba(255, 255, 255, 0.08);
}
```

### 10.3 Tailwind 类

```css
.glass-card        /* 玻璃拟态卡片 */
.neon-text         /* 霓虹文字效果 */
.tag-pill          /* 标签胶囊 */
.source-aggregate-bar /* 聚合提示条 */
.recommendation-bar   /* 推荐理由条 */
.heat-badge        /* 热度徽章 */
.icon-btn          /* 图标按钮 */
```

---

## 十一、未来扩展方向

### 11.1 聚合详情展开

点击"另有 N 个来源"后展示：
- 来源列表
- 相似标题对比
- 时间分布图
- 各来源说法差异

### 11.2 来源分布可视化

在聚合提示区下方增加：
- 来源类型分布饼图
- 时间线分布条
- 地域/语言信息

### 11.3 信号来源追踪

显示：
- 哪个来源最早发
- 哪个来源最权威
- 传播路径图

---

## 十二、可用一段话总结

> 事件卡片采用深色高对比信息板样式，挂接于纵向时间轴右侧，作为单个事件节点的主体承载。每张卡片不对应单篇文章，而对应一个经系统聚合后的事件簇。卡片从上到下依次包含**来源信息、事件标题、摘要说明、语义标签、相似来源合并提示、系统推荐理由以及事件评分与轻操作区**。视觉上以大圆角、轻边框、弱阴影和冷色高亮构成，强调专业、克制、连续追踪的情报雷达气质。交互上，"另有 N 个源也报道了此事件"区域可作为展开入口，用于展示事件簇内的更多来源、相似标题与时间分布信息；底部推荐理由条用于表达系统的筛选逻辑和事件价值判断。

---

## 附录 A: 设计关键词速查

| 关键词 | 含义 |
|-------|------|
| 深色玻璃卡 | 半透明深色背景 + 模糊 |
| 轻悬浮 | 弱阴影 + 轻描边 |
| 霓虹发光 | 青蓝色 box-shadow |
| 大号时间 | text-3xl neon-text |
| 事件聚合 | 多来源合并为事件簇 |
| 推荐理由 | 系统判断与解释层 |
| 信号分 | 综合评分非单纯热度 |

---

**文档状态**: ✅ 完成  
**适用产品**: AI Timeline / 情报雷达 / 事件追踪系统  
**最后更新**: 2026-04-20 14:00
